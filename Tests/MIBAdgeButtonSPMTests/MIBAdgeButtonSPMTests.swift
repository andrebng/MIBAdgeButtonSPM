import XCTest
@testable import MIBAdgeButtonSPM

final class MIBAdgeButtonSPMTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MIBAdgeButtonSPM().text, "Hello, World!")
    }
}
